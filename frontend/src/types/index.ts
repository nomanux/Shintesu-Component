/** The three standard control sizes used across all form components. */
export type ControlSize = "small" | "middle" | "large";

/** A single option in a Select or similar dropdown. */
export type SelectOption = {
  value: string;
  label: string;
};
